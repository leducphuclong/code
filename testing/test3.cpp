#include <iostream>

using namespace std;

int main() {
	char ch;
	cin.get(ch);
	cout << "ch: " << ch << endl;
	cin.get(ch);
	cout << "ch: " << ch << endl;
	cin.get(ch);
	cout << "ch: " << ch << endl;
	return 0;
}