#include <bits/stdc++.h>

using namespace std;

#define int long long
#define nln '\n'

signed main() {
	// Fast IO
	cin.tie(0)->sync_with_stdio(0);
	cout.tie(0)->sync_with_stdio(0);

	// Input
	int t;	cin >> t;
	while (t--) {
		string s;
		cin >> s;
		int k;	cin >> k;
		int n = s.size(), ans = 0;
		map<char, int> cnt;
		int left = 0, right = 0;
		while (right < n) {
			if ô= right - left  + 1;
				...
				...
			} else {
                ...

			}
		}
		cout << ans << nln;
	}

	return 0;
}
