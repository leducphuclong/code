#include <bits/stdc++.h>
using namespace std;
void solve(){
    freopen("in", "r", stdin);
    string str;
    while (getline(cin, str)) {
        cout << str << endl;
    }
}

int main() {
    solve();
    return 0;
}