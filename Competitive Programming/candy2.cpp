#include<iostream>
#include<map>
#include<vector>
#include<set>

#define long long long
#define nln '\n'

using namespace std;

map<long, bool> sum;

int main()
{
	cin.tie(0)->sync_with_stdio(0);
	cout.tie(0)->sync_with_stdio(0);
	long n, m;
	//n = 1, m = 40;
	cin >> n >> m;
	long l = m/2, r = m/2+1;
	if (n % 2) 
		r++;
	long mav = 1e9;
	long man = (1e4+5e3);
	
	for (long i = 1; )
	return 0;	
}