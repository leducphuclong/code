#include<iostream>
#include<vector>
#include<algorithm>

#define long long long
#define nln '\n'

using namespace std;

int main()
{
	cin >> n;
	string s1, s2, res;
	getline(cin, s1, nln);
	getline(cin, s2, nln);
	getline(cin, res, nln);
	s1 = ' ' + s1;
	s2 = ' ' + s2;
	res = ' ' + res;
	vector<long> dp(n+1, 1e9);
	for (long i = n; i >= 1; i++)
		if ()
	return 0;
}