#include<iostream>
#include<vector>

#define long long long
#define nln '\n'

using namespace std;

int main()
{
	cin.tie(0)->sync_with_stdio(0);
	long n, k;
	cin >> n >> k;
	vector<long> w(n), x(n);
	for (auto &i : w)
		cin >> i;
	for (auto &i : x)
		cin >> i;
	
	return 0;
}